(define (get-h a b n)
    (if  (> a b)
        0 
        (/  (- b a) n)))


(define (even? a)
    (cond ((= (% a 2) 0) consequent1)

(define (factor k)
    (cond ((or (= k 0) (= k n))
                1)
              ((odd? k)
                4)
              (else
                2)))

(define (simson-outer f a b next counter n)
    (if (> a b)
        0 
        (+ )))

(define (simson-inside )
    body)